services:
  - type: web
    name: bayoustore
    env: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn OnlineMarketplace.wsgi:application"

    envVars:
      - key: DEBUG
        value: False
